version: 2

sources:
  - name: bronze
    description: Dataset contains table migrated from sources
    schema: bronze

    tables:
      - name: delta_order_items
        identifier: delta_order_items

      - name: orders_delta
        identifier: orders_delta

      - name: delta_products
        identifier: delta_products

      - name: users_delta
        identifier: users_delta

